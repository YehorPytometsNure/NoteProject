import NoteContent from './note-content.js';

export default class NoteContentText extends NoteContent {

  /**
   * Instantiates NoteContentText.
   *
   * @param {string} text - text content.
   */
  constructor(text) {
    super(text);
  }
}
